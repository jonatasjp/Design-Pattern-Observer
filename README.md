# Repositório da implementação do padrão de Projeto Observer

Este repositório contém a implementação do padrão de Projeto Observer, foi desenvolvido em Java.

Este padrão é ideal para ser utilizado em cenários onde determinados eventos de um objeto precisam ser observados por outros objetos.